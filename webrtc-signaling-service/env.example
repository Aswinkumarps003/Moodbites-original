# WebRTC Signaling Service Environment Variables

# Server Configuration
PORT=3007

# JWT Secret (should match your user service)
JWT_SECRET=your-jwt-secret-key-here

# MongoDB URI (if needed for user verification)
MONGODB_URI=mongodb://localhost:27017/moodbites

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
